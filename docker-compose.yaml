version: "3.8"

services:
  pool-db:
    image: postgres:14-alpine
    container_name: pool-db
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - poll-db-data:/var/lib/postgresql/data/pgdata
    env_file:
      - .env
    environment:
      PGDATA: /var/lib/postgresql/data/pgdata

  poll-mongo:
    image: mongo
    container_name: poll-mongo
    restart: always
    ports:
      - "27017:27017"
    env_file:
      - .env
    volumes:
      - mongodb_data:/data/db


  pool-app:
    build:
      context: .
      dockerfile: _configs/Dockerfile
    container_name: pool-app
    command: bash start.sh
    env_file:
      - .env
    environment:
      WAIT-HOSTS: "pool-db pool-redis"
    volumes:
      - ./src:/home/pool_users/app
    ports:
      - "5000:5000"
    tty: true
    restart: on-failure
    depends_on:
      - pool-db
      - poll-mongo

<<<<<<< HEAD


=======
  poll-redis:
    image: "redis:alpine"
    container_name: poll-redis
    env_file:
      - .env
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/bitnami/redis/data

  poll-celery:
    build:
      context: .
      dockerfile: _configs/Dockerfile
    container_name: poll-celery
    command: "celery -A pkg.celery.tasks.celery_worker worker --loglevel=info"
    env_file:
      - .env
    depends_on:
      - pool-app
      - poll-redis
      - pool-db
    volumes:
      - ./src:/home/pool_users/app


  poll-flower:
    build:
      context: .
      dockerfile: _configs/Dockerfile
    container_name: poll-flower
    command: "celery -A pkg.celery.tasks.celery_app flower"
    env_file:
      - .env
    ports:
      - "5555:5555"
    depends_on:
      - poll-redis
    volumes:
      - ./src:/home/pool_users/app

>>>>>>> 79d7435 (Begin to work on dynamic tasks)
#  poll-ui:
#    build:
#      dockerfile: _configs/DockerfileUI
#      args:
#        - GIT_TOKEN=${GIT_TOKEN}
#    container_name: poll-ui
#    ports:
#      - "80:80"
#      - "443:443"
#    env_file:
#      - .env
#    environment:
#      - VUE_APP_BACKEND_URL=http://poll-app:5000

volumes:
  poll-db-data:
  mongodb_data:
  redis_data:


networks:
  pool-network:
    external: false
